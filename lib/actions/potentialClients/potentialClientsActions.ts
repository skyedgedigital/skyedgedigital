import { createPotentialClients } from './create';

const potentialClientActions = {
  CREATE: {
    createPotentialClients: createPotentialClients,
  },
  DELETE: {},
  FETCH: {},
  UPDATE: {},
};

export default potentialClientActions;
